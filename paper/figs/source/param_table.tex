\begin{table}[t]
\centering
\caption{MS-TCM v6 parameter inventory and starting values.
Starting values for the six inherited parameters come from
\citet{CornellZhang2025} Table~1; $\lambda$ starts at the value
recommended by the v6 design notes
(\texttt{notes/two\_level\_cmr\_v6.pdf} \S5). The
\texttt{--standard-tcm} reduction fixes $\lambda = 0$ and disables
the storyline-level context; the remaining six parameters are
optimised. Free-recall-only parameters ($\beta_{\mathrm{rec}}$,
$\varepsilon_{d}$) are ignored in cued-recall paradigms.}
\label{tab:params}
\small
\begin{tabular}{@{}l l l p{0.46\linewidth}@{}}
\hline
Parameter & Identifier & Starting value & Description \\
\hline
$\beta_{\mathrm{enc}}$ & \texttt{beta\_enc} & $0.679$ &
Item-level context drift rate at encoding.
\citep{CornellZhang2025} Table~1. \\
$\beta_{\mathrm{story}}$ & \texttt{beta\_story} & $0.400$ &
Storyline-level context drift rate at encoding (only drifts when the
storyline is active). \citep{CornellZhang2025} Table~1 (labelled
$\beta_{\mathrm{list}}$ there). \\
$\gamma_{\mathrm{fc}}$ & \texttt{gamma\_fc} & $0.315$ &
Weight on the experimental vs pre-experimental input context
(Eq.~\ref{eq:cin}). \citep{CornellZhang2025} Table~1. \\
$k$ & \texttt{k} & $6.50$ &
Softmax gain at retrieval (Eq.~\ref{eq:softmax}).
\citep{CornellZhang2025} Table~1. \\
$\lambda$ & \texttt{lambda\_reinstate} & $0.80$ &
Storyline-return reinstatement strength (Eq.~\ref{eq:lambda}).
\textbf{NEW in MS-TCM}; initialized per
\texttt{notes/two\_level\_cmr\_v6.pdf} \S5. \\
$\beta_{\mathrm{rec}}$ & \texttt{beta\_rec} & $0.326$ &
Within-trial retrieval context drift rate (free-recall only).
\citep{CornellZhang2025} Table~1. \\
$\varepsilon_{d}$ & \texttt{epsilon\_d} & $1.04$ &
Stopping-rule rate (free-recall only).
\citep{CornellZhang2025} Table~1. \\
\hline
\end{tabular}
\end{table}
