# Session notes — 2026-04-21

## Status

Branch `001-ms-tcm-impl` holds a substantially complete implementation of Feature 001
("MS-TCM model + FRFR-category worked example + MLE/CI fitter"). All 44 unit + integration
tests pass locally on macOS; the duplicate-definition gate (`scripts/check_no_duplicate_defs.py`)
reports 57 top-level names across 16 package files with no duplicates and no notebook
redefinitions. Only one commit (`d63b883` "Initial commit") has landed so far — all the
feature work below is currently unstaged/untracked. Pushing this commit will create the
remote branch for the first time.

## What has been built (relative to `specs/001-ms-tcm-impl/tasks.md`)

All tasks in Phases 1–7 are marked `[X]` except:

- **T042** `test_parameter_recovery_on_synthetic_recalls` — the *test file* exists at
  `code/tests/test_parameter_recovery.py` and runs (it produced the scipy RuntimeWarning
  about invalid subtract in the optimizer finite-difference step), but the task is
  still unchecked in `tasks.md`. Confirm the assertion actually pins ≥ 95 % true-inside-CI
  coverage before marking T042 done.
- **T053** cross-platform regression fixture in `code/tests/test_cross_platform.py`
  — fixture file does **not** yet exist. The CI workflow (`.github/workflows/ci.yml`,
  T053b) is in place, but the fixture it depends on still needs to be generated by
  running `ms-tcm fit` on macOS and pasting the MLEs in.

Everything else (T001–T041b, T043–T052, T053b, T054) is implemented.

## Repo layout after this session

```
code/ms_tcm/        model package (model.py, fit.py, bootstrap.py, likelihood.py,
                    context.py, composite.py, similarity.py, schema.py, dataset.py,
                    features.py, frfr.py, mechanisms.py, params.py, io.py, cli.py)
code/tests/         11 test files, 44 tests passing
code/analyses/      clustering.py, embeddings.py, lag_crp.py, pfr.py, predicted.py,
                    serial_position.py
code/figures/       make_fig_{model, experiment, analyses, clustering}.py +
                    make_param_table.py
code/scripts/       compute_embeddings.py (sentence-transformer step used by reproduce.sh)
scripts/            reformat_frfr_category.py, check_no_duplicate_defs.py
data/raw/frfr_category/   bundled Parquet/CSV + manifest.json (self-verifying)
paper/figs/source/  fig_experiment.{pdf,png}, fig_model.{pdf,png} (freshly regenerated)
specs/001-ms-tcm-impl/  spec.md, plan.md, tasks.md, data-model.md, quickstart.md,
                        research.md, contracts/{cli,dataset-schema,fitter,model-api}.md,
                        checklists/
pyproject.toml      declares `ms_tcm` at `code/ms_tcm`, Python 3.11, deps, CLI entry
reproduce.sh        end-to-end reproducer (venv → embeddings → fit → figures → paper)
Dockerfile          rebased onto python:3.11-slim (T004)
CLAUDE.md           project guidance file
.github/workflows/ci.yml   cross-platform test matrix (macos/ubuntu/windows)
```

## Test run (evidence)

```
$ python -m pytest code/tests -q
............................................                             [100%]
44 passed in ... (warnings only — scipy RuntimeWarning about invalid value in
subtract during bootstrap finite-difference gradient, same warning cited in
the T042 notes above)
```

Exit code 0.

## Outstanding work when resuming

1. **Finish T042** — verify the assertion in
   `code/tests/test_parameter_recovery.py` actually tests the "≥ 95 % of parameters
   have their true value inside their bootstrap 95 % CI" criterion from SC-009.
   Address the scipy RuntimeWarning if it indicates an ill-conditioned Hessian at
   the recovery point rather than benign boundary behaviour.
2. **T053 cross-platform regression fixture** — run
   `ms-tcm fit data/raw/frfr_category --out data/processed/fits/mstcm \
      --n-bootstraps 200 --seed 42` on macOS, capture the per-parameter MLEs,
   add them as a regression fixture under `code/tests/test_cross_platform.py`
   keyed by `ms_tcm.__version__` + `manifest.sha256`, and wire that test into
   `.github/workflows/ci.yml`.
3. **Push this branch upstream** — `origin/001-ms-tcm-impl` does not yet exist;
   the first push needs `-u origin 001-ms-tcm-impl`.
4. **T051/T052 README cross-check** — the project-level README has already been
   rewritten (diff in unstaged changes); confirm the GitHub issue URLs (#1 synthetic
   generator, #2 paper writeup) actually exist in `ContextLab/ms-tcm` before
   committing, or open them first.

## Files changed or added this session

Unstaged modifications (6 files, template → project-specific content):
`.gitignore`, `Dockerfile`, `README.md`, `code/README.md`, `code/notebooks/demo.ipynb`,
`data/README.md`.

Untracked additions (the bulk of the feature): the `code/ms_tcm/` package, tests,
analyses, figure scripts, `pyproject.toml`, `reproduce.sh`, `scripts/`, `specs/`,
`CLAUDE.md`, regenerated figure PDFs/PNGs under `paper/figs/source/`, `.github/`
workflow, `.specify/` Spec Kit artifacts, and `.claude/` skill definitions.

Local-only developer state — NOT to be committed (add to `.gitignore` as part of
this commit):

- `.omc/` — session replay, project memory, scheduled-task state
- `.claude/scheduled_tasks.lock` — per-session file lock

`.claude/settings.json` and `.claude/skills/` *are* intended to be shared (project
permissions and Spec Kit skill manifests).

## Guardrails to re-verify before any future refactor

- §4.4 anchor test (`test_section_4_4_numerical_anchor`) — 0.866 (grouped) vs
  0.806 (bridge) to 4 decimals at β_G=β_S=0.5, w_G=0.2, w_S=0.8, m=3.
- `test_shipped_dataset_hashes_match_manifest` — the committed
  `data/raw/frfr_category/` is self-verifying via SHA-256.
- `test_list_boundary_resets_contexts` — c_G and every c_S must be zero at the
  first step of each new list.
- `test_single_storyline_matches_standard_tcm` — K=1 reduction must match
  standard TCM within 1e-12.
- `scripts/check_no_duplicate_defs.py` must stay green (Constitution II).
